
The "decimal" command in general interface mode activates the standard decimal 
symbols for input and output : the symbols are the decimal sequence "1", "2"; 
the prefix and postfix are empty, and the separator is empty unless the rank 
is > 9, when it is "."

